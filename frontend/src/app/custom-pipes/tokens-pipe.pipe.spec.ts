import { TokensPipePipe } from './tokens-pipe.pipe';

describe('TokensPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TokensPipePipe();
    expect(pipe).toBeTruthy();
  });
});
